export class Administrator {

    constructor(public userId: string, public name: string, public password: string, public activeForm: string, public role: string, public status: string) {
        
    }
}
